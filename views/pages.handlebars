<div class="container-fluid d-flex justify-content-center" id="pages">
  {{#each words as |word|}}
  <div>
    <figure>{{word}}</figure>
  </div>
  {{/each}}
  {{#each defs as |def|}}
  <div>
    <figure>{{def}}</figure>
  </div>
  {{/each}} 
</div>