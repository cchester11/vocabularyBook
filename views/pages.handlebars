<div class="container-fluid d-flex justify-content-center" id="pages">
  {{#each words || word}}
  <div>
    <figure>{{word}}</figure>
  </div>
  {{/each}}
  {{#each defs || def}}
  <div>
    <figure>{{def}}</figure>
  </div>
  {{/each}}
</div>